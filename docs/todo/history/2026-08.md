# 2026-08

## 目次

- [2026-08-31 要件定義を開始し、家族 de TODO！ の要件定義書を作成](#2026-08-31-要件定義を開始し家族-de-todoの要件定義書を作成)
- [2026-08-31 要件定義の未決事項8件を確定](#2026-08-31-要件定義の未決事項8件を確定)
- [2026-08-31 基本設計（画面遷移・API仕様・DBスキーマ概要）とデザイントークンを決定](#2026-08-31-基本設計画面遷移api仕様dbスキーマ概要とデザイントークンを決定)
- [2026-08-31 画面遷移図とER図のSVGを生成し、レビュー指摘を修正](#2026-08-31-画面遷移図とer図のsvgを生成しレビュー指摘を修正)
- [2026-08-31 画面ごとの基本設計書8画面を作成](#2026-08-31-画面ごとの基本設計書8画面を作成)
- [2026-08-31 基本設計フェーズの技術検証3件をタスクとして登録](#2026-08-31-基本設計フェーズの技術検証3件をタスクとして登録)

## 2026-08-31 要件定義を開始し、家族 de TODO！ の要件定義書を作成

- AI開発テンプレート（`CLAUDE.md` / `AGENTS.md` / `.claude/` / `.github/` 等）を確認した。テンプレートのプレースホルダ未記入（`<FRONTEND>` 等）、`prisma/AGENTS.md` の運用フローが Cloudflare D1 の前提とズレている可能性、`src/AGENTS.md` がフロント/バック分離構成に未対応、の3点をユーザーへ指摘し、後で決めることにした。
- ユーザーと1問1答形式のヒアリングを行い、家族グループ・ToDo・通知・PWA/UI・運用について要件を確定した。
- ヒアリング中、Backend技術を NestJS から Express へ変更する指示を受けた。当初の指示書では NestJS が学習目的の必須技術だったが、ユーザー本人の明示的な変更指示のため採用した。
- `docs/specs/01_requirements/family-todo/01_家族todo共有アプリ.md` に要件定義書（システム概要〜未決事項の17項目）を作成し、`docs/specs/01_requirements/README.md` に索引リンクを追加した。
- 優先度の段階数、繰り返しタスクの仕様、非登録メンバーの管理方法など8件を「未決事項」として要件定義書に記録した。次回はこれらをユーザーと1問1答で確定させる。

## 2026-08-31 要件定義の未決事項8件を確定

- ユーザーと1問1答形式のヒアリングを行い、要件定義書「17. 未決事項」の8件をすべて確定した。決定内容: 優先度は3段階（高・中・低）で色分け表示／繰り返しは毎日・毎週（曜日指定）・毎月（日付指定）に対応／コメントは家族全員が誰の投稿でも編集・削除可、既読管理なし／非登録メンバーは家族グループ設定画面で事前登録する共有リスト方式で同名不可／家族グループ削除は作成者のみ実行可（確認ダイアログあり、7.1の「全メンバー同一権限」方針の例外として明記）／Googleログインはアカウント・ドメイン制限なし（招待コード/リンクで参加制御）／Backendの実行基盤はCloudflare Workers上でExpressを動かす方式を第一候補とし、詳細は基本設計で確定／AGENTS.mdのプレースホルダ更新は本セッション内で反映する、の8点。
- 決定内容を要件定義書の該当セクション（7.1 家族グループ・ユーザー、7.2 ToDo、9 認証・権限、16 技術的制約）へ反映し、「17. 未決事項」を「未決事項なし」に更新した。
- ユーザーの承認（今回のセッション内で更新する旨の回答）を受け、`AGENTS.md` のプレースホルダ（`<PROJECT_NAME>` / `<PROJECT_SUMMARY>` / `<FRONTEND>` / `<BACKEND>` / `<DATABASE>` / `<DEPLOY_TARGET>` / ローカル起動・認証の記載）を、確定した要件定義の内容で埋めた。デプロイ先の具体構成とローカル開発の起動方法は、コード未着手・基本設計未実施のため「基本設計で確定」「未定」のまま残した。`src/AGENTS.md` にはNestJS固有の記述がなかったため変更していない。

## 2026-08-31 基本設計（画面遷移・API仕様・DBスキーマ概要）とデザイントークンを決定

- デザイントークンを、`mcp__visualize`のモックアップ（ToDoカード・ボタンのサンプル）をユーザーに見せながら1問1答で決定した。背景・中立色はslate/stoneではなく「クリーム/アイボリー寄りのオフホワイト」を選択（暫定値 `background: #FDF8F0`）。プライマリカラーは珊瑚（コーラル、暫定値 `#E8734A`）。見出し・本文フォントは丸ゴシック系「M PLUS Rounded 1c」。角丸は「中くらい（16px相当・`rounded-2xl`相当）」を選択。コードがまだ無いため、正本である`src/app/globals.css`ではなく`DESIGN.md`の「2. デザイントークン」に暫定値として記載し、globals.css作成時に移す旨を明記した。
- モックアップのラベル文字色が薄すぎて読みにくいというフィードバックを受けた。今後`mcp__visualize`でキャプション等の小さいテキストを配置する際は、十分なコントラストの色を使う。
- 要件定義書「13. 画面一覧」の8画面をもとに画面遷移図を作成した。`docs/specs/02_basic-design/画面遷移図.md` + `画面遷移図.01.mmd`。ログイン後は所属グループの有無で「ToDo一覧」と「家族グループ作成・参加」に振り分け、グループ削除・自分の退出時は「家族グループ作成・参加」へ戻る遷移とした。ローカルのDocker Desktopが起動しておらず（`docker info`が接続エラー）、SVGへの変換は未実施。次回Docker起動後に変換コマンドを実行する。
- `docs/specs/02_basic-design/family-todo/`を新設し、`README.md`（索引）・`00_family-todo共通.md`（用語・権限・認証・API共通規約）・`01_データベース.md`＋ER図（`01_データベース.01.mmd`、SVG変換は同様に未実施）・`02_API仕様.md`を作成した。DBは`families`/`users`/`unregistered_members`/`categories`/`todos`/`todo_assignees`/`comments`/`notification_settings`/`push_subscriptions`の9テーブル構成とし、非登録メンバーを担当者に含める場合は`todo_assignees.is_follower`でフォロー役の登録ユーザーを区別する設計にした。APIはリソースごとにREST化し、期限接近・期限超過通知はAPI経由ではなくCloudflare Cron Triggers等の定期処理から送信する想定であることをAPI仕様書の末尾に明記した。
- `docs/specs/02_basic-design/README.md`に機能一覧の索引表を追加し、ディレクトリ構成の例に`02_API仕様.md`を加えた。画面ごとの基本設計書（`10_<画面名>.md`〜）はまだ作成しておらず、次のタスクとして残した。

## 2026-08-31 画面遷移図とER図のSVGを生成し、レビュー指摘を修正

- 前回のセッションではローカルのDocker Desktopが未起動でSVG変換ができなかったため、Docker起動後にユーザーへ一度SVGを共有した。
- ユーザーから2点の指摘を受けた: (1) `画面遷移図.01.mmd`のノードラベルに`\n`をそのまま書いていたため改行が効かず「ToDo一覧画面\n未完了/完了タブ」のように文字列がそのまま表示されていた、(2) iOS Safariかつ未インストール時の案内表示が「ログイン画面」からの遷移としてのみ描かれており、未所属・初回ログインで「家族グループ作成・参加画面」へ向かうユーザーに対する考慮が図から読み取れなかった。
- (1)への対応: mermaidのflowchartノードラベルは`\n`では改行できず`<br/>`が必要なため、該当ノードを`todo_list["ToDo一覧画面<br/>未完了/完了タブ"]`のようにダブルクォート＋`<br/>`表記へ修正した。
- (2)への対応: `iOSインストール案内`を「ログイン画面」からの片方向の一発表示ではなく、「家族グループ作成・参加画面」「ToDo一覧画面」の両方から出入りするオーバーレイとして描き直した（`group_entry -.-> ios_guide -.-> group_entry`、`todo_list -.-> ios_guide -.-> todo_list`）。あわせて`画面遷移図.md`の「遷移の要点」も、未所属・所属済みいずれの場合も同条件で表示し閉じた後は表示元の画面に留まる旨に書き直した。
- `MSYS_NO_PATHCONV=1`無しでは Git Bash から Docker のマウントパスが壊れて`docs/diagrams.md`の注意書きどおり`-i`のファイルが見つからないエラーになったため、`MSYS_NO_PATHCONV=1 docker run --rm -v "$(pwd)":/data minlag/mermaid-cli:11.12.0 ...`の形で実行して解決した。
- `画面遷移図.01.svg`・`family-todo/01_データベース.01.svg`を再生成してコミット対象に含めた。

## 2026-08-31 画面ごとの基本設計書8画面を作成

- 前セッション分（要件定義の確定・基本設計一式・DESIGN.md・TODO/履歴）が未コミットのままだったため、ユーザーの承認を得て `main` へ直接コミット・pushした（`.md` と図の生成物のみのため `AGENTS.md` の paths-ignore 例外に該当）。このリポジトリでは git の user.name / user.email がグローバルにもローカルにも設定されておらずコミットが失敗したため、直近コミットと同じ著者情報を `git -c` でこの1コミットに限り指定した。恒久設定はユーザーに委ねた。
- 要件定義書「13. 画面一覧」の8画面について、`docs/specs/02_basic-design/family-todo/` に `10_ログイン.md` / `12_家族グループ作成・参加.md` / `14_ToDo一覧.md` / `16_ToDo追加・編集.md` / `18_ToDo詳細.md` / `20_家族グループ設定.md` / `22_個人設定.md` / `24_iOSインストール案内.md` を作成した。番号は後から画面を挟めるよう2つずつ空けた。8ファイルとも「画面の目的 / アクセス条件・初期表示 / 表示項目 / 入力チェック / 操作と遷移先 / 権限による差 / エラー時の表示文言 / DBへの影響 / 使用API」の同じ構成にそろえ、後続の単体テスト仕様書が転記できるようエラー文言は実際に表示する文字列をそのまま書いた。
- 要件定義から一意に決まらない点は推測せず、1問1答でユーザーに確認して決めた。決定内容は次のとおり。
  - **文字数上限**: グループ名30 / 表示名20 / 非登録メンバー名20 / ToDoタイトル100 / 詳細メモ1000 / コメント500文字。共通仕様（`00_family-todo共通.md`）に一覧表として集約し、各画面はそれを根拠に入力チェックを書いた。
  - **招待コード**: 半角英数字8桁（大文字）・発行から7日間有効。`families` に `invite_code_expires_at` を追加した。
  - **ToDo一覧の並び順**: 「未完了/完了」タブの下に「期限順/優先度順」タブを置き、タブは初期表示の並び順を決めるだけとする。その中に「並び替える項目（期限/優先度/担当者）」と「昇順/降順」のプルダウンを2つ持ち、プルダウンの指定はタブの並び順とは無関係に効く。並び替えは取得済みデータに対して画面側で行う方式にし、APIに並び替えクエリは持たせなかった（家族数名分の件数しか扱わないため）。
  - **カテゴリ**: 一覧上部の絞り込みチップ（すべて＋6カテゴリ）＋各カードのバッジ表示。
  - **繰り返しToDoの完了**: 新しいToDoを作らず、同じToDoの `due_at` を次回へ進めて未完了のまま残す方式。完了者・完了日時は記録しない。
  - **退出時のToDo**: 退出した人を担当者から外すのみで、ToDo自体はグループに残す（担当者が0人になれば「担当者なし」）。
  - **作成者の退出**: グループ削除できる人が居なくなるのを避けるため、残ったメンバーのうち最も古くから所属している人へ `created_by_user_id` を引き継ぐ。最後の1人が退出した場合はグループごと削除する。
  - **リマインド時間**: 固定の7択（1/3/6時間前・1/2/3/7日前）から選ぶ。既定は1日前。
  - **時刻を指定していない期限の基準時刻**: 既定 20:00 とし、個人設定画面で0:00〜23:00の1時間刻みで変更できるようにした。`users` に `default_due_time` を追加した。
  - **iOSインストール案内**: 画面上部の常設バナー（タップで手順モーダル）。「×」で閉じると7日間表示しない。閉じた日時は端末側にのみ保存しサーバーへは送らない。
- ToDo一覧画面の見た目をユーザーへ確認してもらうため、`mcp__visualize` で決定済みデザイントークン（アイボリー背景・コーラル・16px角丸）に沿ったモバイル画面のモックアップを提示した。前回のフィードバックを踏まえ、小さいラベルにも十分なコントラストの色を使った。
- 決定に伴い既存の設計書も更新した。`01_データベース.md` とER図（`01_データベース.01.mmd`、SVGも再生成）に `families.invite_code_expires_at` と `users.default_due_time` を追加。`02_API仕様.md` に招待コードの有効期限・退出時の作成者引き継ぎ・繰り返しToDoの完了時の挙動・`PATCH /users/me` が `default_due_time` も受け付ける旨・`GET /todos` に並び替えクエリを持たせない旨を追記。`00_family-todo共通.md` に文字数上限の表を追加。`family-todo/README.md` の索引に8画面の行を追加し、`02_basic-design/README.md` の「画面単位のファイルは未着手」の注記と `画面遷移図.md` の「表示条件の詳細は画面設計書で確定する」を確定内容へ書き換えた。
- リポジトリ全体の `.md` の相対リンクを走査して切れが無いことを確認した。既存で残る3件（`docs/diagrams.md` のプレースホルダ2件、`docs/skills/update-todo.md` の記入例1件）はいずれも説明用の例であり、今回の追加分に起因するものではない。

## 2026-08-31 基本設計フェーズの技術検証3件をタスクとして登録

- 要件定義書「16. 技術的制約」で「基本設計フェーズで確定する」としていた技術検証が、`docs/todo/TODO.md` にも設計書にも項目として存在しないことが分かったため、詳細設計に着手する前のタスクとして登録した。詳細設計の前提（Backend の実行基盤・通知の送信方式）が検証結果で変わりうるため、順番を詳細設計より前にした。
- `docs/specs/02_basic-design/README.md` に「インフラ関連事項の技術検証」の節を新設し、次の3件を「何を確かめるか / 確かめ方 / ダメだった場合の代替案 / 結果」の表で書いた。結果はいずれも「未検証」。
  - **検証1**: Cloudflare Workers 上で Express が動くか。検証用の小さな Worker に Express のアプリを載せ、`wrangler dev` とデプロイ後の両方で応答が返るか、D1 のバインディング経由で読み書きできるかを見る。動かない場合は Pages Functions 等の別構成へ切り替え、API の実装方針を決め直す。
  - **検証2**: Web Push（VAPID）のライブラリが Workers ランタイムで動くか。Node.js の `crypto` に依存する部分が動くかどうかが分かれ目。ダメなら Workers の Web Crypto で署名を自前で組み立てるか、通知の送信だけ別基盤へ分ける。
  - **検証3**: 期限接近・期限超過の通知を送る Cron Triggers の実行間隔。5分 / 15分 / 1時間おきを候補に、実際の遅れと無料枠の消費を見て決める。遅れが許容できない場合は送信予定時刻を先に保存しておく方式へ変える。
- 節の見出しは記号を避け（`### 検証1 Cloudflare Workers 上で Express が動くか` 等）、GitHub のアンカーが素直に決まる形にした。
- `docs/specs/02_basic-design/family-todo/02_API仕様.md` の「定期処理（バッチ）」が参照していた「`docs/specs/02_basic-design/README.md` のインフラ関連事項」は該当する節が存在しない参照切れだったため、新設した節へのアンカー付きリンクへ直した。
- `docs/todo/TODO.md` の「次にやること」へ検証3件を 5〜7 として挿入し、詳細設計を 8 へ振り直した。「残っているタスク」にも詳細設計より上に3行足し、「進捗サマリ」と「現在の状態」へ技術検証が未実施である旨を反映した。検証の中身は設計書側にだけ置き、TODO からはリンクするだけにした。
- `README.md` / `README_SIMPLE.md` は変更しなかった。今回の変更は残タスクの登録と設計書内の参照修正だけで、セットアップ手順・コマンド・画面構成・ドキュメント構成のいずれも変わっていないため。
- リポジトリ全体の `.md` の相対リンクを走査して切れが無いことを確認した。既存で残る3件（`docs/diagrams.md` のプレースホルダ2件、`docs/skills/update-todo.md` の記入例1件）は説明用の例であり対象外。
