# 2026-09

## 目次

- [2026-09-01 開発環境を構築（Next.js / Express / Cloudflare D1 / Google OAuth / Web Push の初期セットアップ）](#2026-09-01-開発環境を構築nextjs--express--cloudflare-d1--google-oauth--web-push-の初期セットアップ)
- [2026-09-01 ログイン機能を実装](#2026-09-01-ログイン機能を実装)
- [2026-09-01 家族グループ作成・参加機能を実装](#2026-09-01-家族グループ作成参加機能を実装)
- [2026-09-01 ToDo一覧機能を実装](#2026-09-01-todo一覧機能を実装)
- [2026-09-01 ToDo追加編集機能を実装](#2026-09-01-todo追加編集機能を実装)
- [2026-09-01 ToDo詳細機能を実装](#2026-09-01-todo詳細機能を実装)
- [2026-09-01 家族グループ設定機能を実装](#2026-09-01-家族グループ設定機能を実装)
- [2026-09-01 個人設定機能を実装](#2026-09-01-個人設定機能を実装)
- [2026-09-01 iOSインストール案内機能を実装](#2026-09-01-iosインストール案内機能を実装)
- [2026-09-01 UI見直し（ログイン画面の再実装と7画面の確認）](#2026-09-01-ui見直しログイン画面の再実装と7画面の確認)
- [2026-09-01 単体テストのカバレッジ漏れを補う](#2026-09-01-単体テストのカバレッジ漏れを補う)
- [2026-09-01 既存の単体テストに境界値・異常系の抜けを補う](#2026-09-01-既存の単体テストに境界値異常系の抜けを補う)
- [2026-09-01 結合テスト（Playwright）をログイン・ToDo一覧の2画面で実施](#2026-09-01-結合テストplaywrightをログインtodo一覧の2画面で実施)
- [2026-09-02 結合テスト（Playwright）を家族グループ作成・参加、ToDo追加・編集の2画面で実施](#2026-09-02-結合テストplaywrightを家族グループ作成参加todo追加編集の2画面で実施)
- [2026-09-02 結合テスト（Playwright）をToDo詳細で実施](#2026-09-02-結合テストplaywrightをtodo詳細で実施)
- [2026-09-02 結合テスト（Playwright）を家族グループ設定で実施](#2026-09-02-結合テストplaywrightを家族グループ設定で実施)
- [2026-09-02 結合テスト（Playwright）を個人設定で実施](#2026-09-02-結合テストplaywrightを個人設定で実施)

## 2026-09-01 開発環境を構築（Next.js / Express / Cloudflare D1 / Google OAuth / Web Push の初期セットアップ）

- ディレクトリ構成をユーザーに1問ずつ確認した。決定内容: (1) Frontend/Backendは別リポジトリに分けずモノレポ1つにまとめる（Cloudflareへのデプロイはサブディレクトリ単位のビルド設定で問題なく行えることを説明して合意）、(2) pnpm workspace は `apps/frontend` + `apps/backend` + `packages/shared`（型共有フォルダも最初から作る）、(3) Cloudflare D1のスキーマ管理はPrismaを使わずwranglerのD1マイグレーション機能（生SQL）で管理する。
- `pnpm-workspace.yaml` を新設し、旧来の単一パッケージ用ファイル（ルート `src/`・`tsconfig.json`・`vitest.config.ts`、Prisma不採用のため `prisma/`）を削除した。
- **Backend**（`apps/backend/`）: Express を Cloudflare Workers 上で動かす構成（技術検証の再現。`cloudflare:node` の `httpServerHandler` に Express を載せる）。`wrangler.jsonc` に D1バインディング（プレースホルダの `database_id`）と、通知バッチ用の Cron Triggers（15分おき、詳細設計どおり）を設定した。`GET /api/v1/health` のみ実装（画面・API本体の実装は対象外）。Web Push は `shared/push/vapid.ts` に `configureWebPush(env)` を用意し、VAPID鍵は `.dev.vars.example`（Google OAuth・VAPID用）にまとめた。D1マイグレーションは `apps/backend/migrations/`（wrangler機能。手順は同ディレクトリの `README.md`）。
- **Frontend**（`apps/frontend/`）: `create-next-app`（App Router・`src/`・TypeScript・Turbopack）で作成後、Tailwind CSS v4 + Shadcn/UI を導入した。**shadcn CLIがDESIGN.mdの想定（`style: new-york` / `baseColor: slate`）から`preset`方式（既定 `base-nova`）へ変更されていたため**、現行CLIの既定値をそのまま採用し、DESIGN.mdの記載も実態に合わせて更新した。`components.json`の`aliases`を`src/shared/`配下（DESIGN.md・既存のAGENTS.mdのフィーチャーモジュラー規約に合わせる）に変更し、生成物（`button.tsx`・`sonner.tsx`・`utils.ts`）を移設した。DESIGN.mdの暫定デザイントークン（アイボリー背景・珊瑚プライマリ・16px角丸・ダークモード値)を`globals.css`の`@theme`へ反映し、DESIGN.md側の暫定値ブロックは指示どおり削除した。`next/font/google`でM PLUS Rounded 1c（500/700）を読み込み、`manifest.json`・Web Push受信用の最小サービスワーカー（`public/sw.js`）を追加した。
- `packages/shared` に、基本設計の共通エラーレスポンス形（`{ error: { code, message } }`）を表す`ApiErrorBody`型のみを置いた（他のDTOは未実装のため作成せず）。
- ルートのlint/format/typecheck/test/buildをworkspace対応に更新した（`pnpm -r`での fan-out、`apps/frontend`はNext公式の`next lint`相当を別途実行）。`.github/workflows/ci.yml`にBuildステップを追加し、Postgres想定のコメントブロックを削除した。
- **詰まった点・修正した点**: (1) `create-next-app`が独自の`pnpm-workspace.yaml`/`pnpm-lock.yaml`/`.gitignore`を生成し、ルートのworkspace設定と`.env.local.example`の`!`除外ルールが競合して`.env.local.example`が誤ってignoreされていた → 生成された方を削除しルート`.gitignore`に統合。(2) Express 5系は`@types/express`を別途要求する（旧検証はExpress 4系）。(3) `vitest run`はテスト0件だと非0終了するため、各`vitest.config.ts`に`passWithNoTests: true`を設定。(4) Next.js 16は`apps/frontend/AGENTS.md`を`next dev`実行時に自動生成・上書きする仕様のため、既存ブロックは残したまま末尾にこのプロジェクトの規約を追記した。
- 検証: `pnpm install`→`pnpm lint`/`format:check`/`typecheck`/`test`/`build`すべて成功。`pnpm dev:backend`（wrangler dev）で`GET http://localhost:8787/api/v1/health`が200を返すこと、D1バインディングがプレースホルダの`database_id`のままローカルで問題なく使えることを確認した。`pnpm dev:frontend`をブラウザで開き、ライト/ダーク両方でデザイントークン（アイボリー背景・珊瑚ボタン等）とフォントが反映された画面表示を確認した。
- ドキュメントの整合を取った: `AGENTS.md`/`CLAUDE.md`/`.github/copilot-instructions.md`/`REVIEW.md`/`TESTING.md`/`docs/agent_permissions.md`・各ツールの権限設定ファイル（`.claude/settings.json`・`.vscode/settings.json`・`.codex/rules/project.rules`）の`src/AGENTS.md`・`prisma/AGENTS.md`参照を`apps/frontend/AGENTS.md`・`apps/backend/AGENTS.md`へ張り替え、Prisma前提の記述（`docs/prisma_operations.md`含む）をD1向けに書き換え・削除した。`README.md`/`README_SIMPLE.md`は、コード未着手時代の「テンプレートを新規プロジェクトへコピーする手順」のままだったため、実際のセットアップ手順（`pnpm install`→環境変数のコピー→`pnpm dev:frontend`/`pnpm dev:backend`）を書く内容へ全面的に書き換えた。

## 2026-09-01 ログイン機能を実装

- PR #1（開発環境構築）が未マージだったため、先にユーザー確認のうえ`main`へマージし、最新の`main`から`feature/login`を作成した。最初に着手する機能はAskUserQuestionでユーザーへ確認し、「ログイン画面」に決定した（8画面のうち他画面の認証・セッションの前提になるため第一候補として提示）。
- **DBマイグレーション**（`apps/backend/migrations/0001_create_auth_tables.sql`）: ログインに必要な`users`・`sessions`・`notification_settings`の3テーブルのみ作成した。`users.family_id`は家族グループ機能が未実装のため外部キー制約を付けないnullableカラムに留めた（`families`テーブルは家族グループ機能の実装時に追加する）。
- **Backend**（`apps/backend/src/modules/auth/`）: Googleの認可コードをid_tokenに交換し、JWKS（`jose`パッケージ、新規追加）で署名・発行者・audienceを検証する`google-client.ts`、D1アクセスを担う`repository.ts`（`INSERT ... RETURNING`でユーザー作成と同時にID取得、`db.batch()`で通知設定4種別をseed）、ユースケースの`service.ts`、Expressルーターの`routes.ts`、Zod（新規追加）の`validation.ts`を作成した。共通基盤として`shared/errors/app-error.ts`（`AppError`クラス・`Errors`ファクトリ）、`shared/db/get-db.ts`（D1バインディング取得）、`shared/http/session-cookie.ts`（Cookieの読み書き）を新設した。
- **認証ガードの一元化**（`src/index.ts`）: CORSミドルウェア（`FRONTEND_ORIGIN`のみ許可）、`requireAuth`ミドルウェア（`modules/auth`の`verifySession`を呼び`res.locals.authContext`へ格納）、業務コードの代わりにログイン整形とログ出力を1箇所で行うエラーハンドラを実装した。**設計判断**: API仕様書で`GET /auth/me`・`POST /auth/logout`は「認証必須」と明記されていたため、未ログイン時は200 `{authenticated:false}`ではなく401を返す方式にした（Frontend側は401を「未ログイン」の合図として扱う）。
- 新しい環境変数`FRONTEND_ORIGIN`（CORSの許可オリジン）を`apps/backend/.dev.vars.example`に追加した。実値は`.dev.vars`の中身を読まない方針のため、ユーザー自身に`apps/backend/.dev.vars`への追記を依頼した。
- **Frontend**（`apps/frontend/src/modules/auth/`）: `api-client.ts`（`GET /auth/me`・`POST /auth/google/callback`をfetch、ステータスコードをエラー文言へ変換）、`service.ts`（GoogleのOAuth認可URL組み立て、CSRF対策のstateをsessionStorageで発行・検証、ログイン後の遷移先振り分け）、`ui/login-screen.tsx`・`ui/oauth-callback-screen.tsx`・`ui/google-icon.tsx`を作成した。ルートは`/`（ログイン画面）と`/auth/callback`（Googleからの戻り先）の薄いアダプタとして配線した。ログイン成功後の遷移先（`/todos`・`/family/setup`）はToDo一覧・家族グループ作成/参加画面が未実装のため、パスのみ仮に確定した（実装時にこのパスへ合わせる）。
- **詰まった点**: React 18 Strict Mode（開発時のみ mount→unmount→再mount）で、コールバック画面から渡されたエラー文言をログイン画面が表示できない不具合を作り込んだ。原因は、URLの`error`クエリを読み取った直後（`fetchMe()`の結果を待つ前）に`history.replaceState`でURLを書き換えていたため、1回目の使い捨てmountがURLを先に消費してしまい、生き残った2回目のmountが空振りしていたこと。修正: URLの読み取りは行うが、書き換え（消費）は`fetchMe().then()`内で「このmountがキャンセルされていない」と分かってから行うようにした。ブラウザでの手動確認で発見・修正した（単体テストでは検出できなかった。UI層はTESTING.mdの方針でテスト対象外のため）。
- **ローカル環境の詰まった点**: `pnpm dev:backend`が規約の8787番ではなく8788番で起動し、Frontendからの呼び出しがCORSエラーに見える事象が発生。原因は前回セッションの`wrangler dev`残留プロセスが8787番を握ったままだったこと。ユーザー確認のうえ該当プロセスを終了して解決した（手順は[`docs/todo/notes/wrangler-dev-ローカル起動.md`](../notes/wrangler-dev-ローカル起動.md)に記録）。
- **検証**: `pnpm lint`/`format:check`/`typecheck`/`test`/`build`をルートで実行しすべて成功。単体テスト（Vitest）を追加: Backend（`service.test.ts`＝ログインの新規/既存ユーザー分岐・セッション検証の401化・スライディングセッション延長、`validation.test.ts`）、Frontend（`service.test.ts`＝認可URL組み立て・state検証・遷移先振り分け、`api-client.test.ts`＝ステータスコードごとのエラー文言変換）。ブラウザ（Next.js dev + wrangler dev）で実機確認: 未ログイン時に401→ログインボタン表示（ちらつき無し）、Google拒否/認可コード不正/サーバーエラー/通信断の4種のエラー文言がボタン下に表示されること、ログインボタンがGoogleの認可URL（client_id・redirect_uri・scope・state付き）へ正しく遷移すること（実際のGoogle側の「client_id不足」応答で構造の正しさを確認）。ローカルD1（`wrangler d1 execute --local`）で`INSERT ... RETURNING`・通知設定のseed・有効/期限切れセッションのJOINクエリの動作を直接確認した。curlで`/api/v1/auth/me`（未ログイン401・有効セッション200）・`/api/v1/auth/logout`（204・Cookie失効）・`/api/v1/auth/google/callback`（不正コード400）を確認した。
- **未検証**: 実際のGoogleアカウントでの認可コードフロー完走（本物のGoogle Cloud OAuthクライアントでの同意画面通過）は、この環境に実クレデンシャルが無いため未検証。上記のとおり、URL組み立てとBackend側のエラー処理は個別に確認済み。

## 2026-09-01 家族グループ作成・参加機能を実装

- PR #2（ログイン機能）が未マージだったため、先にユーザー確認のうえ`main`へマージし、最新の`main`から`feature/family-group-setup`を作成した。次に着手する機能はAskUserQuestionでユーザーへ確認し、「家族グループ作成・参加」に決定した（ログイン後、所属グループが無いユーザーの遷移先としてログインと直結するため第一候補として提示）。
- **DBマイグレーション**（`apps/backend/migrations/0002_create_families_table.sql`）: `families`テーブル（`invite_code`・`invite_code_expires_at`・`created_by_user_id`）のみ追加。`users.family_id`は0001で外部キー制約なしのnullableカラムとして作成済みのため変更していない。
- **Backend**（`apps/backend/src/modules/family/`）: authモジュールと同じ構成（`routes.ts`/`service.ts`/`repository.ts`/`validation.ts`/`types.ts`/`index.ts`）で新設した。`POST /families`（新規作成）・`POST /families/join`（招待コード参加）を実装し、`src/index.ts`に`app.use("/api/v1/families", requireAuth)`で認証ガードを追加した。**設計判断**: 招待コード（半角英数字8桁・大文字）は`crypto.getRandomValues`で生成し、`findFamilyByInviteCode`で重複が無いことを確認できるまで最大5回再試行する。「すでに家族グループに所属している」チェック（`CONFLICT`）は作成・参加どちらも入口で共通の純粋関数（`ensureNotAlreadyInFamily`）に切り出した。エラー文言は基本設計書「7. エラー時の表示文言」の記載をそのままBackendのエラーメッセージとして使い、Frontend側で上書きしない方針にした（Frontendは401のみ画面専用の文言に差し替える）。
- **Frontend**（`apps/frontend/src/modules/family/`）: `api-client.ts`（`FamilyError`に`placement: "field" | "top"`を持たせ、400/404は入力欄下・401/409/5xx/通信断は画面上部という設計書の表示場所の違いをそのまま型で表現）、`validation.ts`（グループ名30文字・招待コード半角英数字8桁のクライアント側チェック）、`service.ts`（トースト文言の組み立て）、`ui/family-setup-screen.tsx`（タブで「作る」「参加する」を切替、Shadcn CLIで新規追加した`tabs`/`input`/`label`を使用）を作成した。ルートは`/family/setup`（本体）のほか、招待リンク用に`/join?code=XXXXXXXX`の振り分け専用画面（`ui/join-redirect-screen.tsx`）も追加した。
- **auth モジュールへの小さな拡張**: 招待リンクが未ログインで開かれた場合（`/join` → `/?inviteCode=XXXXXXXX` → Google OAuth → `/auth/callback`）でも招待コードを引き継げるよう、`modules/auth/service.ts`に`storePendingInviteCode`/`consumePendingInviteCode`（sessionStorage、既存のOAuth state保存と同じ方式）を追加し、`getPostLoginPath`に`inviteCode`の第2引数（省略可）を追加した。既存の呼び出し元（引数1つ）は変更不要。`fetchMe`・`MeResult`をauthモジュールの公開APIに追加し、family モジュールから利用できるようにした。
- **スコープ外とした項目**: 基本設計書に記載のある「初回ログイン直後のPush通知許可リクエスト」「iOS Safariでのインストール案内の重ね表示」は、通知設定・iOSインストール案内画面（いずれも未実装の別機能）に依存するため、この画面の実装では見送った。次にこれらの機能へ着手する際に追加する。
- **shadcn CLIの注記**: `pnpm dlx shadcn@latest add form`は本リポジトリのレジストリ設定（`style: base-nova`）ではファイルが生成されなかった（`view form`はメタデータのみ返し中身が空）。react-hook-form + zodによるフォーム化（DESIGN.md「フォームはreact-hook-form + zod + ShadcnのFormを基本構成にする」）を見送り、ログイン画面と同じ`useState`によるコントロールドフォームで実装した（2フィールドのみの単純なフォームのため）。
- **検証**: `pnpm lint`/`format:check`/`typecheck`/`test`/`build`をルートで実行しすべて成功。単体テスト（Vitest）を追加: Backend（`service.test.ts`＝作成・参加の業務ルール・招待コード重複時の再試行・各異常系のAppError、`validation.test.ts`）、Frontend（`validation.test.ts`、`api-client.test.ts`＝ステータスコードごとのエラー文言・表示場所変換、`auth/service.test.ts`に招待コード引き継ぎのケースを追加）。ローカルD1（`wrangler d1 migrations apply --local`でマイグレーション適用後、`wrangler d1 execute --local`でテスト用ユーザー・セッションを直接投入）とブラウザ（Next.js dev + wrangler dev）で実機確認: (1) 未所属ユーザーで`/family/setup`を開き画面が表示されること、(2) グループ名未入力で「グループ名を入力してください。」が表示されること、(3) 「山田家」で作成 → `/todos`へ遷移・DBに`families`行と`users.family_id`の更新が反映されること、(4) 所属済みユーザーが`/family/setup`を開くと`/todos`へ即リダイレクトされること（二重所属防止）、(5) 別ユーザーが不正な招待コードで参加を試み「招待コードが正しくありません。家族に確認してください。」が入力欄下に表示されること、(6) 同ユーザーが正しい招待コード（(3)で発行されたもの）で参加 → `/todos`へ遷移・DBの`family_id`が更新されることを確認した。ポート8787を握っていた前回セッションの`wrangler dev`残留プロセス（PID 13728）はユーザー確認のうえ終了した。
- **未検証**: ToDo一覧画面（`/todos`）が未実装のため、作成・参加成功後の遷移先は404になる（既知。次にToDo一覧を実装する際に解消する）。招待リンク（`/join?code=XXXXXXXX`）経由の遷移・未ログイン時のログイン画面への引き継ぎは、コードレビューと単体テスト（`getPostLoginPath`・`consumePendingInviteCode`）でのみ確認し、ブラウザでのE2E確認は行っていない。上記のとおりPush通知許可リクエスト・iOSインストール案内はスコープ外。

## 2026-09-01 ToDo一覧機能を実装

- PR #3（家族グループ作成・参加機能）が未マージだったため、先にユーザー確認のうえ`main`へマージし、最新の`main`から`feature/todo-list`を作成した。次に着手する機能はAskUserQuestionでユーザーへ確認し、「ToDo一覧」に決定した（ログイン・家族グループ作成/参加それぞれの成功後の遷移先`/todos`が404のままだったため第一候補として提示）。
- **DBマイグレーション**（`apps/backend/migrations/0003_create_todo_tables.sql`）: `categories`（固定6件をseed）・`todos`・`todo_assignees`・`unregistered_members`・`comments`・`push_subscriptions`の6テーブルを一括作成した。ToDo一覧の表示（担当者名の解決・コメント件数の集計）にはER図上の関連テーブルが必要なため、この画面専用の最小構成ではなく詳細設計のスキーマ一式をまとめて作成する判断にした。ToDo追加・編集/家族グループ設定が未実装のため、`todo_assignees`・`unregistered_members`・`comments`は現時点で空のまま。
- **Backend**: `apps/backend/src/modules/todo/`（`routes.ts`/`service.ts`/`repository.ts`/`validation.ts`/`types.ts`/`index.ts`）を新設し、`GET /todos`（status・category_idで絞り込み、担当者・コメント件数を組み立てて返す）・`POST /todos/:id/complete`・`POST /todos/:id/incomplete`を実装した。`apps/backend/src/modules/category/`（`GET /categories`）・`apps/backend/src/modules/push-subscription/`（`POST /push-subscriptions`、同一user_id+endpointは`ON CONFLICT DO NOTHING`で重複登録しない）も新設。`apps/backend/src/modules/family/`に`GET /families/me`を追加した（ToDo一覧のヘッダーに表示するグループ名の取得用）。**設計判断**: グループ所属チェック（未所属→403）を各モジュールに重複実装しないよう、`apps/backend/src/shared/auth/ensure-family-membership.ts`に純粋関数として切り出した（`modules/family`・`modules/todo`の両方から利用。shared→modulesの依存を作らないよう、引数は`AuthenticatedUser`型を直接importせず構造的に互換な型で受ける）。他グループ・存在しないToDoへの完了/未完了操作は、存在の有無を漏らさないためどちらも404（「このToDoは削除されています。」）に統一した。繰り返し設定を持つToDoを完了にしたときの次回分の扱いは、基本設計書で明示的にToDo追加・編集側の責務とされているため、この画面の実装では扱っていない。
- **Frontend**: `apps/frontend/src/modules/todo/`（`api-client.ts`/`service.ts`/`push-subscription.ts`/`ui/todo-list-screen.tsx`/`ui/todo-card.tsx`/`types.ts`/`index.ts`）を新設し、`/todos`ページを追加した。並び替え（期限/優先度/担当者の昇順・降順、値が無いToDoは両順で末尾に集約、同値は作成日時が新しい順にタイブレーク）は基本設計書「3.4」のとおり取得済みデータに対して画面側で行い、タブ・カテゴリ切り替え時のみ`GET /todos`を呼び直す。完了/未完了の切り替えは応答を待たずに楽観的に一覧から表示を消し、失敗時は元に戻してエラートーストを表示する。`shadcn@latest add`で`checkbox`/`select`/`badge`/`dropdown-menu`/`alert-dialog`/`card`を追加した（`button.tsx`は既存カスタマイズを上書きしないよう`--overwrite`を使わず維持）。`apps/frontend/src/modules/auth/`に`logout`、`apps/frontend/src/modules/family/`に`fetchMyFamily`（`GET /families/me`）を追加公開した。
- **DESIGN.mdとの整合の判断**: DESIGN.md「一覧（テーブル）」節はサーバ駆動ソート/ページング前提の管理画面向けテーブル（`SortableTableHead`・`<Table>`）を想定しているが、ToDo一覧はモバイルファーストのカード一覧かつ画面側ソート・ページングなしと基本設計書に明記されているため、同節は適用対象外と判断し、カードUI（`Checkbox`+`Badge`+タブ+`Select`）で実装した。
- **スコープ外とした項目**（次にその機能へ着手する際に追加する）: iOS Safariでのインストール案内の重ね表示（`24_iOSインストール案内.md`が別の候補画面のため）。一覧を下に引っ張るプルダウン更新（ネイティブのタッチジェスチャー実装が必要なため、代わりにヘッダーの再読み込みボタンで代替）。メニューの「家族グループ設定」「個人設定」は未実装画面のためリンク遷移のみ（404になる。ログイン・家族グループ作成/参加が`/todos`をこの画面実装まで404のまま参照していたのと同じ扱い）。
- **不具合修正**: 実装したShadcnの`Select`（`@base-ui/react/select`）で、`<SelectValue />`をそのまま使うと選択値の生の値（`"due"`・`"asc"`等）が表示され、`SelectItem`の日本語ラベルが表示されない不具合をブラウザ確認で発見した。`SelectValue`はchildrenに値→表示内容の変換関数を渡す設計（`<SelectValue>{(value) => label[value]}</SelectValue>`）だったため、並び替え項目・並び順の両セレクトに適用して解消した。
- **ローカル環境の詰まった点**: ブラウザでの動作確認を始めようとしたところ、別セッション（別チャット）が起動していたfrontend（ポート3000）・backend（ポート8787）のdevサーバーをこのセッションのBrowserツールから直接参照できず（セッションごとにネットワークが分離されているため）、かつNext.js 16の仕様上、同じ`apps/frontend`ディレクトリでは別ポートでも二重起動できないと判明した。ユーザーに確認のうえ、該当プロセス（`wrangler dev`の親node.exe、およびその後再出現した子のworkerd.exe）を`taskkill`で終了し、このセッション自身のdevサーバーとして`pnpm dev:backend`/`pnpm dev:frontend`を起動し直して確認を行った。`.claude/launch.json`に恒久的な`backend`エントリ（`pnpm --filter backend dev`、ポート8787）を追加した（従来`frontend`エントリのみだった）。
- **検証**: `pnpm lint`/`format:check`/`typecheck`/`test`/`build`をルートで実行しすべて成功。単体テスト（Vitest、`create-vitest-test`サブエージェント2件で作成）: Backend 51件（`todo/service.test.ts`＝グループ未所属・他グループ/削除済みToDoのAppError・担当者/コメント件数の組み立て、`todo/validation.test.ts`、`family/service.test.ts`に`getMyFamily`のケースを追加、`shared/auth/ensure-family-membership.test.ts`）、Frontend 89件（`todo/service.test.ts`＝並び替え・日付整形・空状態文言等、`todo/api-client.test.ts`＝ステータスコードごとのエラー分岐）。`ui/`配下とブラウザAPI依存の`push-subscription.ts`は方針どおり単体テスト対象外とした。ローカルD1（`wrangler d1 migrations apply --local`でマイグレーション適用後、`wrangler d1 execute --local`でテスト用ToDo・担当者・コメントを直接投入。確認後に削除済み）とブラウザ（Next.js dev + wrangler dev、QA用セッションCookie`qa-test-session-id-0001`を使用）で実機確認: 未完了/完了タブの切り替え、期限順（値なしを末尾に集約）・優先度順（高→中→低表示）の並び替えと選択中のラベル表示、並び替えプルダウンの手動変更、カテゴリチップでの絞り込み（該当なしで専用の空状態文言を表示）、完了チェック→一覧から即座に消える楽観的更新→トースト「完了にしました。」+「元に戻す」、完了タブでの完了者・完了日時表示、未完了に戻す操作、メニューからのログアウト確認ダイアログ（`AlertDialog`）表示を確認した。
- **未検証**: 実際のGoogleアカウントでのログインを経由した「初回ログイン直後」のPush通知許可リクエストの表示タイミング（QAセッションでの直接アクセスでは検証できないため）、実機でのPush通知の受信、iOS Safariでのインストール案内（スコープ外）。ToDo追加・編集画面が未実装のため、追加ボタン・カードタップの遷移先は404のまま（既知。次にToDo追加・編集を実装する際に解消する）。

## 2026-09-01 ToDo追加編集機能を実装

- PR #4（ToDo一覧）が未マージだったため、ユーザー確認のうえmainへマージし、最新のmainから`feature/todo-create-edit`を作成した。対象機能はユーザーに確認し、一覧の追加ボタン先が404のため優先度が高い「ToDo追加・編集」に決定した。
- **Backend**: 既存の`modules/todo`へ`POST /todos`・`GET /todos/:id`・`PATCH /todos/:id`・`PUT /todos/:id/assignees`を追加した。タイトル・詳細メモ・繰り返し・非登録メンバー選択時のフォロー役をZodで検証し、担当者のIDが同じ家族グループに属することも確認する。`modules/family`には、担当者選択肢用の`GET /families/me/members`・`GET /families/me/unregistered-members`を追加した。
- **Frontend**: `TodoFormScreen`と`/todos/new`・`/todos/:id/edit`を追加した。カテゴリ・優先度・期限/時刻・繰り返し・登録/非登録メンバー・フォロー役を入力でき、追加後は一覧へ戻る。編集用の入口は用意したが、遷移元となるToDo詳細画面は未実装のため、次の候補としてToDo詳細を残した。
- Shadcn CLIで詳細メモ入力用の`textarea`だけを追加した。Selectの表示値は既存の不具合対策に合わせ、値から日本語ラベルへ変換するchildren関数を渡した。
- **検証**: `pnpm lint`・`pnpm format:check`・`pnpm typecheck`・`pnpm test`・`pnpm build`がすべて成功した。単体テストはBackend 55件、Frontend 93件成功（追加した`todo/validation.test.ts`でタイトル・繰り返し・フォロー役の入力チェック、`todo/api-client.test.ts`で追加・編集APIの呼び出しを確認）。UI層はTESTING.mdの方針に従い単体テスト対象外とした。ブラウザによる動作確認はdevサーバーを起動していないため未実施。

## 2026-09-01 ToDo詳細機能を実装

- PR #5（ToDo追加・編集）が未マージだったため、ユーザー確認のうえmainへマージし、最新のmainから`codex/todo-detail`を作成した。対象機能はユーザー指定の候補から「ToDo詳細」に決定した。
- **Backend**: 既存の`modules/todo`へToDo削除、コメントの追加・編集・削除APIを追加した。詳細取得には作成者・完了者・担当者・コメントを含め、他グループのToDoやコメントは存在を漏らさない404として扱う。ToDo削除時は関連する担当者とコメントも物理削除する。
- **Frontend**: `/todos/:id`と`TodoDetailScreen`を追加した。期限・繰り返し・担当者・メモ・作成者・完了情報を表示し、完了切り替え、編集遷移、確認ダイアログを経た削除、コメントの追加・編集・削除を実装した。既存の一覧カードからのリンク先404を解消した。
- **検証**: `pnpm lint`・`pnpm format:check`・`pnpm typecheck`・`pnpm test`・`pnpm build`が成功した。VitestはBackend 60件、Frontend 94件が成功した。`wrangler deploy --dry-run`時にはログ保存先への権限警告が出たが、Workerのdry-runビルドは完了した。UI層はTESTING.mdの方針に従い単体テスト対象外とした。ブラウザによる動作確認はdevサーバーを起動していないため未実施。

## 2026-09-01 家族グループ設定機能を実装

- ToDo詳細の実装ブランチをローカル`main`へ取り込んだうえで、`codex/family-group-settings`を作成した。GitHub CLIは未認証のためPR #6のAPI照会はできなかったが、ローカルブランチの内容を確認して続行した。
- **Backend**: `modules/family`へ非登録メンバーの追加・削除、招待コード再発行、グループ退出、作成者だけが実行できるグループ削除のAPIを追加した。退出時は自分の担当を外し、作成者の退出時は最も早く参加した残存メンバーへ作成者を引き継ぐ。最後のメンバーの退出とグループ削除は、ToDo・コメント・担当・非登録メンバーを関連順に削除する。
- **Frontend**: `/family/settings`と`FamilySettingsScreen`を追加した。登録メンバーの表示、非登録メンバーの管理、招待リンクのコピーとコード再発行、確認ダイアログを経た退出・削除を実装した。フォームは既存画面と同様に`useState`で管理し、UI層はTESTING.mdの方針により単体テスト対象外とした。
- **検証**: `pnpm format:check`・`pnpm typecheck`・`pnpm test`は成功した（Backend 62件、Frontend 95件）。`pnpm build`は成功し、Wranglerのdry-runだけホーム配下のログ書き込み権限警告を出した。ブラウザによる動作確認はdevサーバーを起動していないため未実施。

## 2026-09-01 個人設定機能を実装

- PR #7（家族グループ設定）が未マージだったため、ユーザーの承認後にマージし、最新の`main`から`codex/personal-settings`を作成した。個人設定だけを実装範囲とし、iOSインストール案内画面は次の独立した画面として追加していない。
- **Backend**: `modules/settings`を新設し、`GET`・`PATCH /users/me`と`GET`・`PATCH /notification-settings`を追加した。表示名（20文字、空白不可）と日付だけの期限に使う基準時刻を個別に保存し、期限接近通知だけが仕様で定めた7種類のリマインド時刻を受け取れるよう検証する。既存の`notification_settings`・`push_subscriptions`テーブルを利用するため、DBマイグレーションは追加していない。
- **Frontend**: `/settings`と`SettingsScreen`を追加した。表示名・メールアドレス、通知4種類のON/OFF、リマインド時刻、基準時刻、Push通知の端末状態、確認ダイアログを経たログアウトを実装した。通知スイッチは保存失敗時に以前の状態へ戻し、`SelectValue`には値を日本語ラベルへ変換する関数を渡して生の値を表示しない。通知許可時はサービスワーカーから購読情報を取得して既存のPush購読APIへ登録する。
- **UI部品**: 通知のON/OFFに必要な`switch`だけをShadcn CLIで追加した。フォームは既存画面と同じく`useState`で管理し、UI層はTESTING.mdの方針により単体テスト対象外とした。
- **検証**: `pnpm lint`・`pnpm format:check`・`pnpm typecheck`・`pnpm test`・`pnpm build`がすべて成功した。VitestはBackend 80件、Frontend 104件が成功した。ブラウザによる動作確認はdevサーバーを起動していないため未実施。

## 2026-09-01 iOSインストール案内機能を実装

- PR #8（個人設定）はCIの`verify`（typecheck）が失敗しており未マージだった。原因を調べたところ`main`でも同じ失敗が継続しており、今回の変更とは無関係の既存不具合と判明した（Next.js 16の型付きルート機能が生成する`.next/types/`の型`LayoutProps`等を、CIがフルビルドより前に`tsc --noEmit`していたため参照できていなかった）。ユーザー承認のうえ、`apps/frontend`の`typecheck`スクリプトを`next typegen && tsc --noEmit`に変更する別PR（PR #9）を先に作成・マージして`main`を修復し、そのうえでPR #8に`main`をマージしてCIを再実行・成功させてからマージした（[`apps/frontend/package.json`](../../../apps/frontend/package.json)）。この後`main`から`codex/ios-install-guide`を作成し、8画面のうち最後の1画面である「iOSインストール案内」に着手した。
- 基本設計書（`docs/specs/02_basic-design/family-todo/24_iOSインストール案内.md`）を確認したところ、個人設定画面の基本設計（`22_個人設定.md`「3.4 Push通知の受け取り状態」）が定めている「追加のしかたを見る」ボタン（iOSインストール案内のDrawerを直接開く）が、個人設定機能の実装（PR #8）時点ではまだ実装されていなかったため、本機能の一部として合わせて追加した。
- **Frontend**: `apps/frontend/src/modules/ios-install-guide/`を新設した。`service.ts`にUser-Agent判定（`isIosUserAgent`）と表示条件の純粋関数（`shouldShowInstallBanner`。iOS Safari・ホーム画面未追加・「閉じる」から7日以上経過または未閉じ、の3条件）を置き、`storage.ts`に閉じた日時を端末の`localStorage`へ保存・読み込みする関数（保存できない場合は例外を握りつぶして次回も表示するだけにする、仕様どおりの挙動）を置いた。UIは`ui/ios-install-drawer.tsx`（手順を表示する`Drawer`。バナー・個人設定画面の両方から`open`/`onOpenChange`で制御する共通部品）と`ui/ios-install-banner.tsx`（表示条件を満たすときだけ描画する常設バナー。サーバー側の描画とは判定結果が異なり得るため、判定はマウント後にのみ行う）に分けた。Shadcn CLIで`drawer`を追加した（生成直後はPrettier未整形だったため別途整形した）。
- **既存画面への組み込み**: ToDo一覧画面（`todo-list-screen.tsx`）のヘッダー直下、家族グループ作成・参加画面（`family-setup-screen.tsx`）の本文先頭に`IosInstallBanner`を配置した（バナーは通常のブロック要素として挿入し、高さの分だけ本文が自然に下がるようにした。固定配置は使っていない）。個人設定画面（`settings-screen.tsx`）には、iOSでホーム画面未インストールのときだけ表示される「追加のしかたを見る」ボタンと、共通の`IosInstallDrawer`を追加した。
- **ESLintの指摘と対応**: バナーのマウント時判定を`useEffect`内で`setState`する実装に対し、新しいReact Hooks ESLintルール（`react-hooks/set-state-in-effect`）が警告を出した。ブラウザ環境の判定はサーバー側の初回描画とは一致し得ないため、意図的にマウント後へ判定を遅らせる必要があると判断し、理由をコメントに残したうえで該当行のみ`eslint-disable-next-line`で許容した。
- **検証**: `pnpm lint`・`pnpm format:check`・`pnpm typecheck`・`pnpm test`・`pnpm build`がすべて成功した（`.next`を削除したクリーンな状態でCIと同条件のtypecheckも確認）。単体テストを追加: Frontend `ios-install-guide/service.test.ts`（`isIosUserAgent`のiPhone/iPad/Android判定、`shouldShowInstallBanner`の3条件それぞれの分岐と7日境界値）。UIはTESTING.mdの方針に従い単体テスト対象外とした。**未検証**: 本機能が実際に表示される画面（ToDo一覧・家族グループ作成/参加）はログイン（Google OAuth）を経由しないと到達できないため、実クレデンシャルが無いこの環境ではブラウザでの表示確認ができなかった。iOS実機での動作（`navigator.standalone`の判定・バナー/Drawerの見た目）も未検証。

## 2026-09-01 UI見直し（ログイン画面の再実装と7画面の確認）

- `main`から`codex/ui-review`を作成した。対象はTODO「UI見直し」。ログイン画面（ユーザーが用意した4枚の画像を使って再実装）と、それ以外の7画面（コードは変更せずスクリーンショットで確認）の2種類に分けて進めた。
- **ログイン画面の画像**: ユーザーが用意した4枚（PC/スマホの背景写真、PC/スマホの完成モックアップ）の扱いをAskUserQuestionで確認した。決定内容: (1) ファイル名は英数字にリネームする、(2) 完成モックアップ画像はそのまま表示せず、カード（見出し・キャッチコピー・ボタン・ローディング/エラー表示）はDESIGN.mdのトークンとShadcnコンポーネントで再現する（状態変化を焼き込み画像で表現できないため）、(3) モックアップ内の「家＋チェックリスト」アイコンだけは画像から切り出して新規アセット化する。背景写真はsharp（既存の依存関係を流用）でリサイズ・WebP化し、1.4〜2.1MBだった4枚を`login-background-pc.webp`（96KB）・`login-background-mobile.webp`（52KB）・`login-icon.webp`（透過、5KB）の3ファイルに圧縮した（未使用になった元のPNG4枚は削除）。
- **実装**（[`apps/frontend/src/modules/auth/ui/login-screen.tsx`](../../../apps/frontend/src/modules/auth/ui/login-screen.tsx)）: PC/スマホで背景写真を出し分けるため、`next/image`の`getImageProps`で生成したpropsを`<picture><source media="(min-width: 768px)">`へ手で組み立てるArt Direction対応にした（Next公式の対応方法。両方の候補を同時に読み込まず、ブラウザが選んだ1枚だけを取得することを`performance.getEntriesByType('resource')`で確認した）。カードは`bg-foreground/85 text-background`でライト/ダーク両テーマに自動追従する。レイアウトはユーザーとのラウンドトリップで調整した: 当初PC幅でカードを左寄せにしたところ写真の人物と重なる指摘を受け、中央寄せに変更→今度は子どもの顔と重なる指摘を受け、最終的に「水平中央・垂直下寄せ」（モバイル幅と同じ`justify-end`をPC幅にも適用）で確定した。
- **既存不具合の発見・修正**: 実装中、Shadcnの`<Button>`（`px-2.5`指定）でも`padding: 0px`になっている不具合をブラウザの`getComputedStyle`で発見した。原因は[`apps/frontend/src/app/globals.css`](../../../apps/frontend/src/app/globals.css)の`* { padding: 0; margin: 0; }`が`@layer`の外に書かれており、CSSカスケードレイヤーの仕様上Tailwindの`p-*`/`m-*`ユーティリティより常に優先されていたこと（開発環境構築時からの既存バグ、今回の変更とは無関係）。ユーザー承認のうえ`@layer base`で囲んで修正した。この後に撮った7画面のスクリーンショットは、この修正が反映された状態になっている。詳細は[`ローカルD1へのセッション投入によるUI確認.md`](../notes/ローカルD1へのセッション投入によるUI確認.md)。
- **7画面の確認**: Google OAuthの実クレデンシャルが無いため、ローカルD1へテスト用ユーザー・家族グループ・ToDoを直接投入し、`HttpOnly`セッションCookieを使い捨てのローカルHTTPサーバー経由でブラウザへ載せてログイン済み状態を再現した（手順は同note）。家族グループ作成・参加・ToDo一覧・ToDo詳細・ToDo編集・家族グループ設定・個人設定・iOSインストール案内（バナー+Drawer、`navigator.userAgent`をiPhoneへ上書きして表示条件を満たした）の7画面をライト/ダーク両方でスクリーンショットし、ユーザーに確認を依頼した。結果は「問題なし」。確認後、投入したテストデータ（子→親の順）とヘルパーサーバーを削除した。
- **検証**: `pnpm lint`・`pnpm format:check`・`pnpm typecheck`・`pnpm test`（Backend 80件・Frontend 113件）・`pnpm build`がすべて成功した。7画面はコード変更が無いため既存の単体テストへの影響も無い。
- **未実施**: このセッション内でのコミット・PR作成はユーザーの最終承認待ち（[`docs/development/gitの操作ルール.md`](../../development/gitの操作ルール.md)のとおりPR作成前に承認が必要なため）。

## 2026-09-01 単体テストのカバレッジ漏れを補う

- TODO「単体テストを作成・実行する」の最初の一歩として、8機能実装時に作成済みの単体テストと、TESTING.mdが★必須とする対象（`service.ts`・`validation.ts`）を突き合わせ、テストが未作成のファイルを洗い出した。見つかったのは2件: [`apps/frontend/src/modules/family/service.ts`](../../../apps/frontend/src/modules/family/service.ts)（トースト文言生成。作成済みの`service.test.ts`一覧に家族グループ機能だけ抜けていた）と、[`apps/backend/src/modules/push-subscription/validation.ts`](../../../apps/backend/src/modules/push-subscription/validation.ts)（Push購読登録の入力検証。この機能だけ`service.ts`が無く`validation.ts`のみのためテストが漏れていた）。
- `main`から`test/unit-test-coverage-gaps`を作成し、`create-vitest-test`サブエージェントへ2件のテスト作成を委譲した。family/service.test.tsは正常系1件×2関数（TESTING.mdの「service向け観点チェックリスト」どおり分岐が無いため代表ケースのみ）、push-subscription/validation.test.tsは正常系1件+3フィールド×未指定/空文字/空白のみの異常系9件（validation.tsの境界値・必須チェックの観点）を作成した。実装コード側の修正は不要だった。
- **検証**: `pnpm --filter frontend test`（10ファイル115件）・`pnpm --filter backend test`（10ファイル90件）がいずれも既存分を含め全件成功。[PR #12](https://github.com/koekoebaborak27/family-todo/pull/12)を作成した（未マージ）。
- **未実施**: 既存テストファイルそれぞれの境界値・異常系の網羅性そのもののレビュー（新規ファイルの有無ではなく、各テストの中身の充足度）はまだ行っていない。次回はここから着手する。

## 2026-09-01 既存の単体テストに境界値・異常系の抜けを補う

- 前項の続き。`general-purpose`サブエージェントへ、TESTING.mdの「service向け観点チェックリスト」を基準に全モジュール（backend: auth/todo/family/settings/push-subscription/shared、frontend: auth/todo/family/settings/ios-install-guide）の`service.ts`・`validation.ts`とテスト内容を突き合わせる監査を依頼した。結果、25件の抜けが見つかった。最も深刻だったのは[`apps/backend/src/modules/todo/service.ts`](../../../apps/backend/src/modules/todo/service.ts)で、`createTodo`・`getTodo`・`updateTodo`・`updateTodoAssignees`・`updateComment`の5関数が丸ごと未テストだった（`todo/service.test.ts`の`describe`一覧を実際に読んで抜けを検証済み）。
- 監査結果をモジュールごとに5分割し、`create-vitest-test`サブエージェントを並列起動して補った: (1) backend todo（service 11件・validation 21件）、(2) backend family（`getMyFamilyMembers`・`getMyUnregisteredFamilyMembers`の未テスト・`leaveMyFamily`の非作成者退出ケース・`createUnregisteredMemberSchema`の境界値）、(3) backend settings（`due_soon`のバリデーション分岐・displayName境界値・refineエラー）、(4) frontend todo（`recurrenceDetailLabel`・`formatCreatedInfo`・`formatCommentDate`の未テスト）、(5) frontend family/settingsの表示名20文字境界値。
- **詰まった点**: `apps/backend/src/modules/settings/validation.test.ts`と`apps/frontend/src/modules/family/service.test.ts`（前項）で、サブエージェントが追記した箇所がPrettierの整形ルールに沿っておらずCIの`Format check`が失敗した。`npx prettier --write`で個別に直し、再pushしてCIを通した。
- **検証**: `pnpm lint`・`pnpm format:check`・`pnpm typecheck`・`pnpm test`（全workspace）がすべて成功（backend 134件・frontend 127件、既存分含め回帰なし）。[PR #13](https://github.com/koekoebaborak27/family-todo/pull/13)を作成し、CI（verify）成功後にユーザー承認を得てマージした。
- これにより、TESTING.mdが★必須とする`service.ts`・`validation.ts`・権限判定の純粋関数は全モジュールで観点チェックリストを満たした。`actions.ts`・`repository.ts`は方針上「△要所のみ」（複雑なクエリ・配線確認のみ）のため、今回は対象外とした。

## 2026-09-01 結合テスト（Playwright）をログイン・ToDo一覧の2画面で実施

- TODO「結合テスト（Playwrightによる画面テスト）を行う」に着手。8画面いきなり全部ではなく、ユーザーの希望でログイン画面とToDo一覧画面の2画面（AskUserQuestionで「両方」を選択）に絞って進めた。
- **単体テスト仕様書の作成**（`create-unit-test-spec`スキル）: [`docs/test/unit/spec/auth/UT_10_ログイン.md`](../../test/unit/spec/auth/UT_10_ログイン.md)（8ケース）と[`docs/test/unit/spec/todo/UT_14_ToDo一覧.md`](../../test/unit/spec/todo/UT_14_ToDo一覧.md)（39ケース、ユーザーの希望で0件パターンを多めに・Push通知許可も対象に含めた）を作成した。仕様書作成中、設計書[`14_ToDo一覧.md`](../../specs/02_basic-design/family-todo/14_ToDo一覧.md)「7. エラー時の表示文言」の記載（401/403は「トースト表示後に画面遷移」）が実装（[`todo-list-screen.tsx`](../../../apps/frontend/src/modules/todo/ui/todo-list-screen.tsx)。トーストを出さず即座に画面遷移）と食い違っていることを発見し、ユーザー承認のうえ設計書側を実装に合わせて修正した。
- **Playwrightテストコードの作成・実行**（`playwright-evidence-test`スキル）: `@playwright/test`は導入済みだった。ログイン方法は、実Googleアカウントの認可コードフローがクレデンシャルの都合で使えないため、[`ローカルD1へのセッション投入によるUI確認.md`](../notes/ローカルD1へのセッション投入によるUI確認.md)と同じ割り切り（D1へユーザー・セッションを直接投入）で代替することをスキルの安全確認ゲートで改めてユーザーへ確認し、承認を得た。テストデータ（家族グループ・ToDo・非登録メンバー・コメント）は生SQLではなく実際のBackend REST APIを呼んで用意し、入力検証を経由させた。共通ヘルパーは[`e2e/support/`](../../../e2e/support/)（`db.ts`＝`wrangler d1 execute --local`の一時ファイル経由呼び出し、`seed.ts`＝ユーザー・セッションの投入と後始末、`api.ts`＝Backend REST APIクライアント、`scenario.ts`＝カード・並び替え等のPlaywright向けヘルパー、`evidence.ts`＝エビデンス保存先）に切り出した。テストコードは[`e2e/auth/ut-10-login.spec.ts`](../../../e2e/auth/ut-10-login.spec.ts)と[`e2e/todo/ut-14-todo-list.spec.ts`](../../../e2e/todo/ut-14-todo-list.spec.ts)。
- **詰まった点**: (1) ルートが`"type": "module"`のため`__dirname`が使えず`import.meta.url`に置き換えた。(2) Windows上で`wrangler d1 execute --command`にSQLを直接渡すと`cmd.exe`が丸括弧等を誤解釈したため、一時ファイル経由の`--file`に変更した。(3) 複数テストが並列にローカルD1へ書き込むと`FOREIGN KEY constraint failed`が発生したため、`playwright test --workers=1`で直列実行にした。(4) `getByRole("tab", { name: "完了" })`が「未完了」タブにも部分一致し、`getByRole("alert")`もNext.jsのroute-announcer用divと衝突したため、`exact: true`やCSSセレクタでの絞り込みに変更した。(5) `window.location.href`への実遷移をabortするとブラウザのエラーページに遷移し元ページの`sessionStorage`が読めなくなったため、Google認可URLへの遷移検証はリクエストURLのパラメータ確認のみに留めた。
- **結果**: ログイン8ケース全て成功。ToDo一覧は39ケース中38ケースを自動化し37ケース成功、Push通知の購読登録（`PushManager.subscribe()`が実際のプッシュ配信サービスへの到達を要する）はこのローカル検証環境のネットワーク到達性の制約により未実施（アプリの不具合ではないと判断し、テストコード側で`test.skip`により記録）。エビデンス（スクリーンショット・`result.md`）は[`docs/test/unit/result/auth/テスト結果UT_10_ログイン/`](../../test/unit/result/auth/テスト結果UT_10_ログイン/result.md)と[`docs/test/unit/result/todo/テスト結果UT_14_ToDo一覧/`](../../test/unit/result/todo/テスト結果UT_14_ToDo一覧/result.md)に保存した（`.gitignore`対象のためリポジトリには残らない）。
- テストコードが投入したテストデータ（`google_sub`が`e2e-`接頭辞のユーザー・セッション・家族グループ等）は`test.afterAll`で自動削除する実装にした。実行後、ローカルD1に`e2e-`接頭辞のデータが残っていないこと、既存の開発データ（`test-sub-qa`等・`山田家`）に影響が無いことを確認済み。
- **未実施**: 残り6画面（家族グループ作成・参加／ToDo追加・編集／ToDo詳細／家族グループ設定／個人設定／iOSインストール案内）の結合テストは未着手。

## 2026-09-02 結合テスト（Playwright）を家族グループ作成・参加、ToDo追加・編集の2画面で実施

- 前回の続きとして、残り6画面のうちユーザーが選んだ家族グループ作成・参加、ToDo追加・編集の2画面を実施した。
- **単体テスト仕様書の作成**（`create-unit-test-spec`スキル）: [`docs/test/unit/spec/family/UT_12_家族グループ作成・参加.md`](../../test/unit/spec/family/UT_12_家族グループ作成・参加.md)（20ケース）と[`docs/test/unit/spec/todo/UT_16_ToDo追加・編集.md`](../../test/unit/spec/todo/UT_16_ToDo追加・編集.md)（33ケース）を作成した。仕様書作成中、設計書と実装の食い違いを3点発見し、ユーザー承認のうえ実装どおりにテストケースを書いた（詳細は各仕様書「7. 補足」）: (1) 家族グループ作成・参加画面はPush通知許可リクエストを実装していない（ToDo一覧画面側で行っている）、(2) ToDo追加・編集画面の「キャンセル」は変更有無にかかわらず確認ダイアログなしで即座に戻る、(3) ToDo追加・編集画面のエラー表示は項目別ではなく画面上部の単一メッセージにまとめて表示される。
- **Playwrightテストコードの作成・実行**（`playwright-evidence-test`スキル）: 前回と同じくD1へのセッション直接投入でログインを代替し、`e2e/support/`を流用した。テストコードは[`e2e/family/ut-12-family-setup.spec.ts`](../../../e2e/family/ut-12-family-setup.spec.ts)（20ケース）と[`e2e/todo/ut-16-todo-form.spec.ts`](../../../e2e/todo/ut-16-todo-form.spec.ts)（33ケース）。招待コード期限切れ・グループ重複所属の409応答は、API経由では再現できないため`context.route`でサーバー応答をモックして再現した（前回のサーバーエラー再現と同じ手法）。
- **重大な発見**: ToDo追加・編集画面の「編集の保存」テストで、実ブラウザからの`PUT /todos/:id/assignees`が毎回`net::ERR_FAILED`で失敗する現象を発見。Node.jsの直接リクエストでは再現せず、Playwrightのブラウザコンテキストから`PUT`を単独送信しても100%失敗することから、CORS設定の不備と特定した。[`apps/backend/src/index.ts`](../../../apps/backend/src/index.ts)の`Access-Control-Allow-Methods`に`PUT`が含まれていなかったことが原因で、**実際のGoogleアカウント・実際のブラウザでもToDoの担当者編集が常に失敗する状態だった**。ユーザー承認のうえ`PUT`を追加する1行修正を行い、修正後は全ケース安定して成功することを確認した。
- **詰まった点**: (1) `getByLabel("招待コード")`がタブパネルの`aria-labelledby`にも一致しstrict modeエラーになった → `getByRole("textbox", { name: "招待コード" })`に変更。(2) タブの選択状態を`data-state`属性で確認しようとしたが、実装（Base UIのTabs）は`aria-selected`属性を使っていた → 属性名を修正。(3) `test.beforeAll`でのユーザー参加判定にAPI応答ボディの有無を使っていたが、エラーボディがtruthyに評価され参加処理がスキップされていた → 常に参加APIを呼ぶよう単純化。(4) フォロー役のチェック操作が担当者一覧と同じ表示名でstrict modeエラーになった → `.last()`で後方（フォロー役側）を指定。(5) React Strict Modeによる二重実行でトーストが2件表示されるケースがあった → `.first()`で対応。
- **結果**: 家族グループ作成・参加20ケース、ToDo追加・編集33ケースとも全件成功。既存のログイン・ToDo一覧のテストと合わせて通しで再実行し、回帰が無いことを確認した（98件成功・1件スキップ〔Push通知、既知の環境制約〕）。エビデンスは[`docs/test/unit/result/family/テスト結果UT_12_家族グループ作成・参加/`](../../test/unit/result/family/テスト結果UT_12_家族グループ作成・参加/result.md)と[`docs/test/unit/result/todo/テスト結果UT_16_ToDo追加・編集/`](../../test/unit/result/todo/テスト結果UT_16_ToDo追加・編集/result.md)に保存した（`.gitignore`対象のためリポジトリには残らない）。
- コミットは現在のブランチ`test/e2e-login-todo-list`（ログイン・ToDo一覧の結合テストと合わせて1つのPR想定、未マージ）にまとめた: CORS修正のみを1コミット、仕様書・テストコード一式をもう1コミットに分けた。
- **未実施**: 残り4画面（ToDo詳細／家族グループ設定／個人設定／iOSインストール案内）の結合テストは未着手。

## 2026-09-02 結合テスト（Playwright）をToDo詳細で実施

- 前回の続きとして、残り4画面のうちユーザーが選んだ順序（ToDo詳細→家族グループ設定→個人設定→iOSインストール案内）でToDo詳細から着手した。
- **単体テスト仕様書の作成**（`create-unit-test-spec`スキル）: [`docs/test/unit/spec/todo/UT_18_ToDo詳細.md`](../../test/unit/spec/todo/UT_18_ToDo詳細.md)（31ケース）を作成した。仕様書作成中、設計書[`18_ToDo詳細.md`](../../specs/02_basic-design/family-todo/18_ToDo詳細.md)と実装の食い違いを3点発見し、ユーザーに個別に方針を確認した: (1) 完了者・完了日時の表示（設計書は時刻あり、実装は時刻なし）→ユーザーの希望で設計書どおり（時刻あり）を実装した、(2) 繰り返しToDoを完了操作したときの「次回の期限へ進める」処理（設計書に記載があるが未実装で常に完了扱いになっていた）→ユーザーの希望で設計書どおりに実装した、(3) コメント投稿・編集・削除時の401・通信エラーの扱い（設計書は専用メッセージ＋ログイン画面遷移を求めるが、実装は一律の汎用メッセージのみ）→実装どおりとし設計書側を修正した。
- **実装**: (1)向けに[`formatCompletedInfoWithTime`](../../../apps/frontend/src/modules/todo/service.ts)を追加（ToDo一覧カード用の`formatCompletedInfo`は時刻なしのまま維持）。(2)向けに[`apps/backend/src/modules/todo/recurrence.ts`](../../../apps/backend/src/modules/todo/recurrence.ts)（JST基準の次回期限計算。[`10_繰り返しToDoの期限計算.md`](../../specs/03_detail-design/family-todo/10_繰り返しToDoの期限計算.md)どおり）を新設し、`completeTodo`が繰り返し設定を判定して`due_at`を進めるよう変更した。`POST /todos/:id/complete`の応答を`204`から`200 { recurring, nextDueAt }`に変更し、ToDo詳細・ToDo一覧の両画面のトースト表示（「次回は{日付}です。」）を対応させた。単体テスト（Vitest）で次回期限計算の境界値（毎日・毎週の同一曜日のみ選択・毎月の月末はみ出し・年またぎ）を検証した。
- **Playwrightテストコードの作成・実行**（`playwright-evidence-test`スキル）: 前回と同じくD1へのセッション直接投入でログインを代替し、`e2e/support/`を流用した。テストコードは[`e2e/todo/ut-18-todo-detail.spec.ts`](../../../e2e/todo/ut-18-todo-detail.spec.ts)（31ケース）。
- **重大な発見**: コメントの編集（`PATCH /api/v1/comments/:id`）・削除（`DELETE /api/v1/comments/:id`）が**実装当初から常に500エラーになっており、一度も正しく動作していなかった**ことを発見。原因は[`apps/backend/src/index.ts`](../../../apps/backend/src/index.ts)で`requireAuth`ミドルウェアが`/api/v1/todos`配下にしか適用されておらず、`todoRouter`が持つ`/comments/:id`宛のルートが素通りして`res.locals.authContext`未設定のままハンドラに到達し`TypeError`になっていたため（PlaywrightテストのTC-017・TC-020が失敗したことで発覚。wranglerのエラーログで特定）。ユーザー承認のうえ`app.use("/api/v1/comments", requireAuth)`を追加して修正し、該当テストが成功することを確認した。前回のPR #14で見つけた`PUT`メソッドのCORS不備と同様、実ブラウザ経由の結合テストで初めて表面化した不具合。
- **詰まった点**: (1) `getByText`の完全一致なし検索がテストデータのタイトル文字列（例:「期限なし」を含むタイトル）と部分一致してstrict modeエラーになった → `{ exact: true }`を指定。(2) ToDo本体の「削除」ボタンとコメントの「削除」ボタンが同文言でstrict modeエラーになるケースがあった → DOM順が先のヘッダー側を`.first()`で指定。(3) コメント編集モードでは表示テキストがtextareaの値に変わるため`hasText`フィルタが編集後に一致しなくなりタイムアウトした → コメントが1件だけの前提で`.first()`に変更。(4) 画面の初期読み込み前にAPIでToDoを削除すると`GET /todos/:id`が404を返し詳細表示自体が出ないままタイムアウトした → 表示完了を待ってから削除するよう順序を修正。
- **結果**: 31ケース全て成功。単体テスト（Vitest）はbackend 142件・frontend 130件（今回追加分含む）全件成功、lint/format/typecheck/buildも通過。エビデンスは[`docs/test/unit/result/todo/テスト結果UT_18_ToDo詳細/`](../../test/unit/result/todo/テスト結果UT_18_ToDo詳細/result.md)に保存した（`.gitignore`対象のためリポジトリには残らない）。
- コード（バックエンド・フロントエンド）とドキュメントが混在するため、featureブランチ`test/e2e-todo-detail`を切り、[PR #15](https://github.com/koekoebaborak27/family-todo/pull/15)としてCI（verify）成功を確認のうえユーザー承認を得て`main`へマージした（マージ後にブランチ削除済み）。
- **未実施**: 残り3画面（家族グループ設定／個人設定／iOSインストール案内）の結合テストは未着手。

## 2026-09-02 結合テスト（Playwright）を家族グループ設定で実施

- 前回の続きとして、残り3画面のうちユーザーが選んだ順序で家族グループ設定に着手した。
- **単体テスト仕様書の作成**（`create-unit-test-spec`スキル）: [`docs/test/unit/spec/family/UT_20_家族グループ設定.md`](../../test/unit/spec/family/UT_20_家族グループ設定.md)（32ケース。グループ退出は他メンバー残存／作成者引き継ぎ／最後の1人の3パターンを含む）を作成した。仕様書作成中、設計書[`20_家族グループ設定.md`](../../specs/02_basic-design/family-todo/20_家族グループ設定.md)と実装の食い違いを3点発見し、ユーザーに個別に方針を確認した: (1) 画面表示時（初回読み込み）の401・403はトーストなしの即遷移→ToDo一覧・ToDo詳細と同じ既存パターンのため実装どおりとした、(2) グループ削除403のサーバー側エラー文言（設計書「グループを作成した人だけです。」、実装「作成者だけです。」）→サーバー文言だけ設計書に合わせて修正、(3) 各操作（招待コード再発行・退出・グループ削除・非登録メンバーの追加・削除）中の401（セッション失効）が専用メッセージ・ログイン画面遷移をせず操作別の汎用失敗メッセージのみだった→設計書どおりに実装を修正する方針とした。
- **実装**: (2)向けに`apps/backend/src/modules/family/service.ts`のエラー文言を修正。(3)向けに`family-settings-screen.tsx`へ共通処理`handleUnauthorized`（401なら専用トースト＋`/`へ遷移）を追加し、招待コード再発行・退出・グループ削除・非登録メンバーの追加・削除の5つの操作ハンドラすべてに適用した。
- **Playwrightテストコードの作成・実行**（`playwright-evidence-test`スキル）: 前回と同じくD1へのセッション直接投入でログインを代替。テストケースごとに専用の家族グループ・ユーザーを都度作る方針（退出・削除で状態が壊れるテストが多いため）にし、テストコードは[`e2e/family/ut-20-family-settings.spec.ts`](../../../e2e/family/ut-20-family-settings.spec.ts)（32ケース）。
- **詰まった点**: (1) メンバー行の「(あなた)」「(作成者)」表示は実装上、表示名の直後に半角スペースを挟んでから括弧が続く（`{displayName} (あなた)`）仕様だったが、最初はスペース無しでテストを書いてしまい`getByText`が一致しなかった → 仕様書・テストコードともスペースありに修正して解消。(2) 非登録メンバーの「削除」ボタンと「グループを削除する」ボタンが、Playwrightの`getByRole`の既定（部分一致）でともに「削除」にヒットしstrict modeエラーになった → `{ exact: true }`を指定して解消。
- **結果**: 32ケース全て成功。単体テスト（Vitest）・lint・format・typecheck・buildもすべて通過。エビデンスは[`docs/test/unit/result/family/テスト結果UT_20_家族グループ設定/`](../../test/unit/result/family/テスト結果UT_20_家族グループ設定/result.md)に保存した（`.gitignore`対象のためリポジトリには残らない）。今回は新規の実装不具合の発見は無かった（前回のToDo詳細で見つかったコメントAPIの500エラーとは別モジュール）。
- featureブランチ`test/e2e-family-settings`を切り、[PR #16](https://github.com/koekoebaborak27/family-todo/pull/16)としてCI（verify）成功を確認のうえユーザー承認を得て`main`へマージした（マージ後にブランチ削除済み）。
- **未実施**: 残り2画面（個人設定／iOSインストール案内）の結合テストは未着手。

## 2026-09-02 結合テスト（Playwright）を個人設定で実施

- 前回の続きとして、残り2画面のうちユーザーが選んだ順序で個人設定に着手した。
- **単体テスト仕様書の作成**（`create-unit-test-spec`スキル）: [`docs/test/unit/spec/settings/UT_22_個人設定.md`](../../test/unit/spec/settings/UT_22_個人設定.md)（24ケース）を作成した。仕様書作成中、設計書[`22_個人設定.md`](../../specs/02_basic-design/family-todo/22_個人設定.md)と実装の食い違いを2点発見し、ユーザー承認のうえ設計書どおりに実装を修正した: (1) 表示名の保存・通知設定の切り替え・リマインドのタイミング変更・基準時刻の変更・Push許可の各操作中の401（セッション失効）が専用メッセージ・ログイン画面遷移をせず操作別の汎用失敗メッセージのみだった（ToDo詳細・家族グループ設定で見つかったのと同じパターン）、(2) Push通知の購読情報登録に失敗したときのメッセージが「拒否された」場合と区別されておらず、設計書が定める「通知の設定に失敗しました。」が実際には表示されない状態だった。
- **実装**: `settings-screen.tsx`へ共通処理`handleUnauthorized`を追加し、5つの操作ハンドラすべてに適用した。`handleRequestPushPermission`を、許可リクエスト自体の結果と許可後の購読登録処理を分けて`catch`するよう修正し、購読登録失敗時に正しいメッセージを表示するようにした。
- **Playwrightテストコードの作成・実行**（`playwright-evidence-test`スキル）: 前回と同じくD1へのセッション直接投入でログインを代替。テストコードは[`e2e/settings/ut-22-settings.spec.ts`](../../../e2e/settings/ut-22-settings.spec.ts)（24ケース）。
- **重大な発見（テスト側の不整合）**: e2eテストの共通ヘルパー`createSeedUser`（`e2e/support/seed.ts`）が、実際のGoogleログイン時に行われる通知設定4種別の初期データ投入（`apps/backend/src/modules/auth/repository.ts`の`createUserWithDefaultNotificationSettings`）を再現しておらず、テスト用ユーザーは常に「通知すべてOFF」の状態から始まっていたことを発見した。既存の他のe2eテストは通知設定のON/OFFを確認しないため、この画面のテストを追加するまで気づかれていなかった。実際のsignup時と同じ既定値を投入するよう`createSeedUser`を修正し、他のe2eテスト（ログイン）で回帰が無いことを確認した。アプリ本体の不具合ではなくテスト側の不整合のため、TODOの「発見した不具合」とは区別して記録する。
- **詰まった点**: (1) Switch（Base UI）は`id`プロパティを可視のスイッチ本体ではなく内部の隠しinputへ渡す実装だったため、`#notification-todo_added`のようなID指定では正しい要素を掴めなかった → `getByRole("switch")`をJSXのループ順で位置指定する方式に変更し、ON/OFF判定も`data-state`ではなく`aria-checked`属性に変更して解消。(2) リマインドのタイミング選択と基準時刻選択の2つの`select-trigger`を`.first()`/`.last()`で区別しようとして取り違えた → 表示文言（「◯日前」を含むか）で区別する方式に変更して解消。(3) 通知設定・Push購読登録の失敗（500）を`context.route`で再現する際、対象メソッド以外（CORSプリフライトのOPTIONS等）まで500にしてしまい、実際のリクエストがブラウザ側で止まった → 対象メソッド以外は`route.continue()`するよう修正して解消。(4) `context.grantPermissions`を画面表示前に呼ぶと初回描画時点で許可済み扱いになり「通知を許可する」ボタン自体が表示されなくなった → ボタン表示を確認してから許可を確定させる順序に変更して解消。
- **結果**: 24ケース中23ケース成功、1ケース（Push通知の購読登録成功パス）はローカル検証環境からブラウザのPush Serviceへ到達できず`test.skip`で記録（前回・前々回のPush通知テストと同じ既知の環境制約）。単体テスト（Vitest）・lint・format・typecheck・buildもすべて通過。エビデンスは[`docs/test/unit/result/settings/テスト結果UT_22_個人設定/`](../../test/unit/result/settings/テスト結果UT_22_個人設定/result.md)に保存した（`.gitignore`対象のためリポジトリには残らない）。
- featureブランチ`test/e2e-settings`を切り、[PR #17](https://github.com/koekoebaborak27/family-todo/pull/17)としてCI（verify）成功を確認のうえユーザー承認を得て`main`へマージした（マージ後にブランチ削除済み）。
- **未実施**: 残り1画面（iOSインストール案内）の結合テストは未着手。これで8画面中7画面が完了。
